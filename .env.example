# タイムゾーン設定
TZ=Asia/Tokyo

# バックエンドAPIの公開ポート
BACKEND_PORT=8080

# フロントエンドの公開ポート
FRONTEND_PORT=3000

# フロントエンドから接続するバックエンドAPIのURL
# クライアント・サーバー両方で使用（NEXT_PUBLIC_プレフィックス必須）
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Docker内部通信用のバックエンドAPIのURL（オプション）
# 設定した場合、Next.jsサーバー側（RSC/Server Actions）ではこちらを優先使用
# 未設定の場合はNEXT_PUBLIC_API_URLを使用
# Docker Compose環境: http://backend:8080/api/v1
# 本番環境（別々デプロイ）: 設定不要（NEXT_PUBLIC_API_URLと同じになるため）
API_URL=http://backend:8080/api/v1
