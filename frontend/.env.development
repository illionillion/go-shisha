# pnpm dev 時（NODE_ENV=development）に自動で読み込まれる
# Codespaces の場合は .env.local で BACKEND_URL=https://<workspace>-8080.app.github.dev に上書き
BACKEND_URL=http://localhost:8080

# 以下は機密情報のため .env.local に設定すること（gitignore対象）
# echo "REDIRECT_SECRET=$(openssl rand -hex 32)" >> frontend/.env.local
# REDIRECT_SECRET=
