# .env.local.example → .env.local にコピーして使用（gitignore対象）

# ログイン後リダイレクト先の暗号化キー（必須・本番では必ず強力なランダム文字列に変更）
# 以下のコマンドで生成:
# echo "REDIRECT_SECRET=$(openssl rand -hex 32)" >> frontend/.env.local
REDIRECT_SECRET=

# 本番イメージをローカルでDockerビルドする場合のみ設定（CI/CDではARGで渡す）
# BACKEND_URL=http://<バックエンドコンテナ名>:8080
